# LFS-Development-workspace
Configured an OS development workspace for Linux From Scratch (LFS v13.0) on an Ubuntu host via VirtualBox. Managed disk partitioning, established the cross-compilation toolchain, and mounted kernel virtual file systems (proc, sysfs, dev) to isolate the chroot environment for safe, low-level system compilation completely from source.
